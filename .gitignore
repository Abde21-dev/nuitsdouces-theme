# Fichiers système
.DS_Store
Thumbs.db

# Cache FTP deploy
.ftp-deploy-sync-state.json

# Dépendances éventuelles (si tu ajoutes npm plus tard)
node_modules/

# Fichiers sensibles — ne jamais commiter
*.env
.env.*
